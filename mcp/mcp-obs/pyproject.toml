[project]
name = "mcp-obs"
version = "0.1.0"
description = "MCP server for observability (VictoriaLogs and VictoriaTraces)"
readme = "README.md"
requires-python = "==3.14.*"
dependencies = [
    "httpx>=0.28.1",
    "mcp>=1.26.0",
]
